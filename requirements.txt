# Web app
Flask==3.1.3
requests==2.34.2
python-dotenv==1.2.3

# Optional AI coaching (app runs fine without a key -- see .env.example)
openai==3.2.0

# Local sentiment/emotion analysis (no API key needed, runs on your machine)
transformers==5.15.0
torch==2.13.0

# Tests
pytest==9.1.1
